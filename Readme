使用说明
1. Global.py定义了程序运行的数据读取、权重读取、输出结果保存的路径
以及集成学习中使用的两个权重的比例。在使用test.py前，先将修改路径为
实际路径，再运行test.py
2. 直接运行test.py，程序就可完成数据和权重的导入，并完成对测试集的
一次推理，推理结果保存在Global.py定义的路径下
3. 文件介绍
	Global.py：定义程序运行所需的路径
	utils.py：定义了导入数据类，以及pytorch实现的处理3D数据的DenseNet(性能不佳，未再使用)
	test.py：导入数据和权重，完成一次推理。
	train_and_eval.py：模型训练


